# =====================================================
# ProductLens AI — Backend Environment Configuration
# =====================================================
# NOTE: Gemini API Keys are NEVER stored here.
# They are passed dynamically from the user's browser
# via the Authorization header on each request.
# =====================================================

# Port for the Express server
PORT=3001

# Temp directory for intermediate video files (relative to backend root)
TEMP_DIR=./temp
